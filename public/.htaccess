RewriteEngine On

# Serve existing files and directories directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Vite SPA fallback for React Router routes.
RewriteRule ^ index.html [L]
